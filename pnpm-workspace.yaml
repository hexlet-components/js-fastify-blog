# pnpm с 10-й версии не запускает build-скрипты зависимостей без явного
# разрешения. sqlite3 собирает нативный модуль: Sequelize грузит его по имени
# диалекта в рантайме, и без сборки падает не установка, а запуск.
allowBuilds:
  sqlite3: true
