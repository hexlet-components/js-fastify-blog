# pnpm с 10-й версии не запускает build-скрипты зависимостей без явного
# разрешения. esbuild собирает нативный модуль, и без сборки ломается не
# установка, а запуск: на нём стоит сборка ассетов.
allowBuilds:
  esbuild: true
